using ClassLibrary1;

namespace ConsoleApp14
{
    internal static class Program
    {
        private static void Main(string[] args)
        {
            Method(Mode.Mode1);
        }

        private static void Method(Mode mode = Mode.Mode1)
        {
        }
    }
}
